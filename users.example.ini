; Template for users.ini (which is git-ignored and holds the real credentials).
; Copy this file to users.ini and fill in real users.
;
; One section per group. Each line inside a section: email = password
; Add more users by adding more lines, or more groups by adding a new
; [section-name] header.

[test]
max.mustermann@pkg-online.de = mm_1234

[lk_ph_abi28]
vorname.nachname@pkg-online.de = vn_5678
